/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Future configuration for serving static assets or other specific Next.js settings can go here.
  // For example, if you plan to use images from external domains:
  // images: {
  //   domains: ['example.com'],
  // },
};

module.exports = nextConfig;
